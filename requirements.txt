# --- Core (required to run the full pipeline + dashboard) ---
# streamlit is capped below 1.42: newer builds (e.g. 1.58) import
# starlette.App via a path that breaks on the bundled starlette, so the
# dashboard fails to import. The <1.42 range is the tested, working window.
streamlit>=1.30,<1.42
scikit-learn>=1.3.0,<2.0
pandas>=2.0.0,<3.0
numpy>=1.24.0,<3.0
plotly>=5.18.0,<7.0

# --- Tests (the README/Pi guide run `pytest`) ---
pytest>=7.4.0,<9.0

# --- Optional: physical Raspberry Pi sensors (NOT required for the demo) ---
# Climate Mesh runs fully without these. Install them only when connecting a
# real Vernier Go Direct Weather sensor / MQ-7 air-quality channel.
#   pip install godirect adafruit-blinka adafruit-circuitpython-ads1x15
# They are commented out so a fresh clone installs cleanly on any machine.
#
# IMPORTANT — two pieces are needed, and only one is a pip package:
#   * `godirect`  -> the low-level Vernier driver (this pip package).
#   * `gdx`       -> Vernier's high-level helper MODULE that the adapter imports
#                    (`from gdx import gdx`). It is NOT on PyPI; copy the `gdx/`
#                    folder from https://github.com/VernierST/godirect-examples
#                    into the project root. See docs/hardware_driver_setup.md.
# godirect
# adafruit-blinka
# adafruit-circuitpython-ads1x15
